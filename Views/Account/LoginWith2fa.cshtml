@model IdentityServer4.Models.AccountViewModels.LoginWith2faViewModel
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <base href="../../../" />
    <title>Xác thực hai yếu tố - Đăng nhập</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="shortcut icon" href="~/assets/media/logos/favicon.ico" />
    <link href="~/font/inter/inter.css" rel="stylesheet" />
    <link href="~/assets/plugins/global/plugins.bundle.css" rel="stylesheet" />
    <link href="~/assets/css/style.bundle.css" rel="stylesheet" />
    <link href="~/css/login.css" rel="stylesheet" />
</head>
<body id="kt_body" class="app-blank">
    <div class="d-flex flex-column flex-root">
        <div class="d-flex flex-column flex-lg-row flex-column-fluid">
            <div class="d-flex flex-column flex-lg-row-fluid w-lg-50 p-10 order-2 order-lg-1">
                <div class="d-flex flex-center flex-column flex-lg-row-fluid">
                    <div class="login-wrap">
                        <partial name="_ValidationSummary" />
                        <form asp-action="LoginWith2fa" method="post">
                            <input type="hidden" asp-for="ReturnUrl" />
                            <input type="hidden" asp-for="RememberMe" />
                            <div class="text-center mb-11">
                                <h1 class="text-gray-900 fw-bolder mb-3">Xác thực hai yếu tố</h1>
                                <p class="text-gray-500 fs-6">Nhập mã từ ứng dụng xác thực của bạn</p>
                            </div>
                            <div class="fv-row mb-5">
                                <label class="d-block mb-1">Mã xác thực <span class="text-danger">*</span></label>
                                <input asp-for="TwoFactorCode" class="form-control bg-transparent" autocomplete="one-time-code" autofocus placeholder="000000" maxlength="7" />
                            </div>
                            <div class="form-check mb-5">
                                <input asp-for="RememberMachine" class="form-check-input" />
                                <label asp-for="RememberMachine" class="form-check-label">Ghi nhớ thiết bị này</label>
                            </div>
                            <div class="d-grid mb-10">
                                <button type="submit" class="btn btn-primary">Xác thực</button>
                            </div>
                            <div class="text-center">
                                <a asp-action="LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">Sử dụng mã khôi phục</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            <div class="d-flex flex-lg-row-fluid w-lg-50 bgi-size-cover bgi-position-center order-1 order-lg-2 bg-auth">
                <div class="d-flex flex-column flex-center py-7 py-lg-15 px-5 px-md-15 w-100">
                    <img alt="Logo" src="~/assets/media/logos/custom-1.png" class="h-60px mb-10" />
                    <h1 class="text-white fs-2qx fw-bolder text-center mb-7">Xác thực hai yếu tố</h1>
                    <p class="text-white fs-base text-center">Bảo vệ tài khoản của bạn</p>
                </div>
            </div>
        </div>
    </div>
    <script src="~/assets/plugins/global/plugins.bundle.js"></script>
    <script src="~/assets/js/scripts.bundle.js"></script>
</body>
</html>
